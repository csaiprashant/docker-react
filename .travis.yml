sudo: required
services:
        - docker

before_install:
        - docker build -t csaiprashant/docker-react -f Dockerfile.dev .

script:
        - docker run -e CI=true csaiprashant/docker-react npm run test

